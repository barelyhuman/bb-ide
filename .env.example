# Copy this file to .env and uncomment only the values you need to override.

# Runtime state. Defaults:
# - pnpm start: ~/.bb
# - pnpm dev: ~/.bb-dev
# BB_DATA_DIR=~/.bb

# Production-mode ports used by pnpm start.
# BB_SERVER_PORT=38886
# BB_HOST_DAEMON_PORT=38887

# Human-facing and externally reachable URLs. Leave empty for local-only use.
# BB_APP_URL=
# BB_EXTERNAL_URL=

# Optional provider/API credentials used by server-side features and sandbox hosts.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# E2B_API_KEY=
# E2B_TEMPLATE=
# BB_GITHUB_PAT=

# Server-side inference model for generated titles, commit messages, and similar helpers.
# BB_INFERENCE_MODEL=openai/gpt-4o-mini
